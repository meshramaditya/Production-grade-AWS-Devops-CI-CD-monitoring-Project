apiVersion: v1
kind: Service
metadata:
  name: react-service
  namespace: react-devops
spec:
  type: LoadBalancer

  selector:
    app: react-app

  ports:
    - port: 80
      targetPort: 80
      protocol: TCP